<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.common.pms" >

    <application
        android:name="com.example.common.pms.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".AdminLogin"
            android:label="Admin Login">

        </activity>
        <activity
            android:name=".StudentLogout"
            android:label="Student Login">

        </activity>
        <activity
            android:name=".AllProductsActivity"
            android:label="All Product">

        </activity>

        <activity
            android:name=".NewProductActivity"
            android:label="New Product">

        </activity>

        <activity
            android:name=".AddDriveActivity"
            android:label="Add Drive">

        </activity>

        <activity
            android:name=".AdminHome"
            android:label="Login">

        </activity>

        <activity
            android:name=".StudentHome"
            android:label="St Login">

        </activity>

        <activity
            android:name=".ViewDrivesActivity"
            android:label="View Drives">

        </activity>

        <activity
            android:name=".ViewProfileActivity"
            android:label="View Profile">

        </activity>



        <activity
            android:name=".StudentLogin"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" >

        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
